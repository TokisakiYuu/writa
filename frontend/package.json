{
  "name": "yuulog-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && webpack --config webpack.config.js",
    "build-profile": "npm run clean && webpack --config webpack.config.js --json > compilation-stats.json",
    "clean": "rimraf dist",
    "dev": "npm run clean && webpack --config webpack.config.js --watch"
  },
  "author": "tokisakiyuu",
  "license": "MIT",
  "devDependencies": {
    "awesome-typescript-loader": "^5.2.1",
    "rimraf": "^3.0.2",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "webpack": "^5.1.3",
    "webpack-cli": "^4.1.0"
  },
  "dependencies": {}
}
